# Shared UI Components

No shared UI component directory or reusable Blade components exist yet. The repository is a fresh Laravel 12 starter. The requested Kioosk homepage is therefore a new UI target rather than a redesign of an existing component system.

The only frontend entry point is `resources/views/welcome.blade.php`, Laravel's self-contained default welcome screen; it does not expose reusable primitives.
